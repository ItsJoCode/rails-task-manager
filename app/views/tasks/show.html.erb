<h2><%= @task.title %> - <%= link_to "Delete", task_path(@task),
    data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}%></h2>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>
<%= link_to "Edit Task", edit_task_path(@task) %>
<br>
<%= link_to "Back to list", tasks_path %>
