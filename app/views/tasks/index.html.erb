<h1>My Tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li> <% if task.completed %>
         [X]
         <% else %>
         [ ]
         <% end %>
         <%= link_to task.title, task_path(task)%></li>
  <% end %>
</ul>

<%= link_to "Create new Task", new_task_path%>
